{
  "name": "random-bool",
  "version": "0.0.0",
  "description": "Generate a random boolean (true/false).",
  "main": "index.js",
  "scripts": {
    "lint": "jshint index.js",
    "pretest": "npm run lint",
    "test": "mocha -R spec",
    "test:cov": "rm -rf ./test/coverage && istanbul cover _mocha --dir ./test/coverage -- -R spec",
    "test:rpt": "npm run test:cov && coveralls < ./test/coverage/lcov.info",
    "prepublish": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bubkoo/random-bool.git"
  },
  "keywords": [
    "generator",
    "generate",
    "gen",
    "random",
    "randomize",
    "rand",
    "bool",
    "boolean"
  ],
  "author": "bubkoo",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/bubkoo/random-bool/issues"
  },
  "homepage": "https://github.com/bubkoo/random-bool#readme",
  "devDependencies": {
    "jshint": "^2.9.1",
    "chai": "^3.5.0",
    "coveralls": "^2.11.9",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5"
  }
}
